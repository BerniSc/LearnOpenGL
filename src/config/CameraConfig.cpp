#include "config/CameraConfig.hpp"

CameraConfig::CameraConfig(const float& YAW, const float& PITCH, const float& SPEED, const float& SENSITIVITY, const float& ZOOM) :
    yaw(YAW), pitch(PITCH), speed(SPEED), sensitivity(SENSITIVITY), zoom(ZOOM) {

}
